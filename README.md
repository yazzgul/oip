# Курс: Основы информационного поиска
# Студент: Язгуль Хасаншина, 11-203



## Задание 1 (директория task01): Веб-краулер (сбор текстовых страниц)

### Описание
Скрипт на Python загружает не менее 100 веб-страниц из предварительно подготовленного списка URL (файл `urls.txt`). Каждая страница сохраняется в отдельный текстовый файл вместе с HTML-разметкой. Создаётся индексный файл `index.txt` с соответствием номеров файлов и исходных ссылок.

**Цель задания**: получить корпус русскоязычных веб-страниц для дальнейшего анализа.

### Требования к окружению
- Python 3.7 или выше
- Библиотека `requests` (устанавливается через pip)

### Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd ваш-репозиторий/task01

## Задание 2 (директория task02): Токенизация и лемматизация

### Описание
Скрипт на Python выполняет обработку текстовых файлов, содержащих токены и леммы. На вход подаются три файла: `tokens_1.txt` (список токенов), `lemmas_1.txt` и `lemmas_2.txt` (словари лемм с словоформами). На выходе формируются два очищенных файла: список уникальных токенов и группировка токенов по леммам.

**Цель задания**: получить очищенный корпус токенов и их лемматизированное представление для дальнейшего лингвистического анализа.

### Требования к окружению
- Python 3.7 или выше
- Никаких дополнительных библиотек не требуется (используется только стандартная библиотека)

### Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd ваш-репозиторий/task02


## Задание 3 (директория task03): Инвертированный индекс и булев поиск

### Описание
Скрипт на Python строит инвертированный индекс для коллекции текстовых документов и реализует систему булева поиска с поддержкой сложных запросов. Индекс сохраняется в JSON-файл. Программа позволяет вводить запросы в интерактивном режиме с использованием операторов AND, OR, NOT и круглых скобок.

**Цель задания**: освоить базовые принципы информационного поиска — построение инвертированного индекса и реализацию булевой модели поиска.

### Требования к окружению
- Python 3.7 или выше
- Никаких дополнительных библиотек не требуется (используется только стандартная библиотека: `re`, `json`, `os`, `collections`)

### Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd ваш-репозиторий/task03
2. При запуске программа предложит ввести булев запрос. Можно ввести, например:
```bash
   (Клеопатра AND Цезарь) OR (Антоний AND Цицерон) OR Помпей
```
   Или нажать Enter для выполнения демонстрационного запроса.

## Задание 4 (директория task04): Вычисление TF-IDF

### Описание

Скрипт на Python выполняет вычисление TF-IDF для корпуса документов, полученного в `task01`.  
Обработка проводится для всех текстовых файлов из директории `downloaded_pages`.

Для каждого документа:

- вычисляется TF, IDF и TF-IDF для всех терминов;
- вычисляется TF, IDF и TF-IDF для лемматизированных форм (на основе файлов `lemmas_1.txt` и `lemmas_2.txt`);
- создаются отдельные выходные файлы с результатами.

Результаты сохраняются в директории `task04` -> директории `results` в папках:
- `terms`
- `lemmas`

**Цель задания**: реализовать вычисление статистических весов терминов и лемм для корпуса документов и подготовить данные для последующего ранжирования и анализа.

---

### Требования к окружению

- Python 3.7 или выше
- Используется стандартные библиотеки Python, BeautifulSoup, pymorphy3
---

### Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd ваш-репозиторий/task04

   
